<?xml version="1.0" encoding="UTF-8"?>
<sequence name="get-specific-policy-admin-papi-sequence" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    <propertyGroup description="apiProperty">
        <property name="CorrelationID" expression="get-property('correlation_id')"/>
        <property name="LatencyMs" scope="default" type="STRING" value="0"/>
        <property name="StartTime" expression="get-property('SYSTEM_TIME')"/>
        <property name="policyNumber" expression="get-property('uri.var.policyNumber')"/>
    </propertyGroup>
    <call-template target="Common-Log-Template-V1">
        <with-param name="service" value="policy-api"/>
        <with-param name="sequence" value="get-specific-policy-admin-papi-sequence"/>
        <with-param name="level" value="INFO"/>
        <with-param name="message" value="Started get-specific-policy-admin-papi-sequence"/>
        <with-param name="status" value="START"/>
    </call-template>
    <propertyGroup description="inputProperty">
        <property name="HTTP_METHOD" scope="axis2" type="STRING" value="GET"/>
        <property name="REST_URL_POSTFIX" scope="axis2" type="STRING" expression="fn:concat('/api/', get-property('policyNumber'))"/>
    </propertyGroup>
    <call>
        <endpoint key="policy-sapi-restEndpoint"/>
    </call>
    <property description="statusCode" expression="$axis2:HTTP_SC" name="statusCode" scope="default" type="STRING"/>
    <filter xpath="get-property('statusCode')='200'">
        <then>
            <script language="js"><![CDATA[

                var payload = mc.getPayloadJSON();

                function removeNull(obj) {
                    if (obj == null) return null;

                    if (Array.isArray(obj)) {
                        return obj.map(removeNull);
                    }

                    if (typeof obj === "object") {
                        var newObj = {};
                        for (var key in obj) {
                            if (obj[key] !== null && obj[key] !== "") {
                                newObj[key] = removeNull(obj[key]);
                            }
                        }
                        return newObj;
                    }

                    return obj;
                }

                function mapPolicy(p) {
                    return removeNull({

                        parties: p.parties,

                        quote: p.quote,

                        policyNumber: p.policyNumber,
                        policyAcceptDate: p.policyAcceptDate,
                        policyissuedDate: p.policyissuedDate,
                        policyCancellationType: p.policyCancellationType,
                        policyCancellationComments: p.policyCancellationComments,
                        policyCancelledDate: p.policyCancelledDate,
                        policyStatusCode: p.policyStatusCode,
                        productCode: p.productCode,
                        termStartDate: p.termStartDate,
                        termEndDate: p.termEndDate,
                        policyPeriod: p.policyPeriod,
                        transactionIdentifier: p.transactionIdentifier,
                        transactionTypeCode: p.transactionTypeCode,
                        currentPolicyTermIdentifier: p.currentPolicyTermIdentifier,
                        transactionStatusCode: p.transactionStatusCode,
                        termStatusCode: p.termStatusCode,

                        driverDetails: p.driverDetails,
                        underWritingQuestions: p.underWritingQuestions,
                        insurableObjects: p.insurableObjects,
                        coverages: p.coverages,
                        pricing: p.pricing,
                        documents: p.documents,
                        billing: p.billing,
                        renewals: p.renewals,
                        reinstatements: p.reinstatements,
                        endorsements: p.endorsements
                    });
                }

                var result;

                if (Array.isArray(payload)) {
                    result = payload.map(mapPolicy);
                } else {
                    result = mapPolicy(payload);
                }

                mc.setPayloadJSON(result);

            ]]></script>            
        </then>
        <else>
            <propertyGroup description="error details">
                <property expression="json-eval($.errors[0].errorCode)" name="ERROR_CODE" scope="default" type="STRING"/>
                <property expression="json-eval($.errors[0].errorMessage)" name="ERROR_MESSAGE" scope="default" type="STRING"/>
                <property expression="json-eval($.errors[0].errorDescription)" name="ERROR_DETAIL" scope="default" type="STRING"/>
            </propertyGroup>
            <call-template description="Error Log" target="Common-Log-Template-V1">
                <with-param name="service" value="policy-api"/>
                <with-param name="sequence" value="get-specific-policy-admin-papi-sequence"/>
                <with-param name="level" value="INFO"/>
                <with-param name="message" value="{$ctx:ERROR_MESSAGE}"/>
                <with-param name="status" value="ERROR"/>
            </call-template>
            <sequence key="policy-admin-papi-fault-sequence"/>            
        </else>
    </filter>    
    <property name="LatencyMs" expression="get-property('SYSTEM_TIME') - get-property('StartTime')"/>
    <call-template target="Common-Log-Template-V1">
        <with-param name="service" value="policy-api"/>
        <with-param name="sequence" value="get-specific-policy-admin-papi-sequence"/>
        <with-param name="level" value="INFO"/>
        <with-param name="message" value="Ended get-specific-policy-admin-papi-sequence"/>
        <with-param name="status" value="END"/>
    </call-template>
</sequence>